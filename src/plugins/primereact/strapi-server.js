
'use strict';

module.exports = () => ({
  register({ strapi }) {
    // Registration logic for the server-side part of the plugin
    // This is where you would register controllers, services, etc.
  },
  bootstrap({ strapi }) {
    // Bootstrap logic for the server-side part of the plugin
  },
});
